# elm-magic 0.8 — 사용자 관점 프리뷰

> **상태: 설계 프리뷰.** 여기 코드는 구현 전 목표 사용자 경험(UX)이다. API 이름·시그니처는
> 확정이 아니며, 0.8 확정 시 `CHANGELOG.md`와 `README.md`가 최종 계약이 된다.
>
> **불변 원칙**
> - 런타임 의존성 **0** (`cargo tree -p elm-magic -e normal` = 매크로 + `enum_dispatch`)
> - **상태는 변수, 이벤트는 대입, 화면은 함수 본문, 효과는 `<-`, 플랫폼은 인자**
> - 잘못된 태그/속성/값은 **컴파일 에러**, 에러 메시지는 DSL 언어로
>
> **기반**: edition 2024, MSRV 1.85 (let-chains 사용 시 1.88)

---

## 0. 0.7 → 0.8 한눈에

| 영역 | 0.7 | 0.8 |
|---|---|---|
| 조건/반복 | `{if c { <Text>..</Text> } else { .. }}` / `{items.map(\|i\| ..)}` | `<If>` `<For>` `<Switch>` + `<>` |
| 자식 값 | `Vec<Element>` / `.map()` 강제 | `IntoView`: `{title}` `{maybe_user}` `{items}` |
| 입력 | `value={x.clone()} on_change={x = _}` | `<Input bind={x} />` |
| 보간 | `"{n}"` | `"{user?.name ?? \"게스트\"}"` |
| 효과 | `a, b <- f(..)` | `<-` + **async 클로저** + `action!` 재사용 본문 |
| 클래스 | `class="a b"` 고정 | `cls!("a", "a--on": cond)` |
| 스타일 | 클래스만 | `style!{ pad(8).gap(4).fill() }` + `css!` 확장(`@media`/변수/`@apply`) |
| 태그 확장 | 5곳 수정 | `define_tag!` / `variants!` / `widgets!` |
| 어댑터 | 변형 `match` 2벌 | `Bridge` 1회 구현 |
| 에러 | rustc 트레이트 에러 | `#[diagnostic::on_unimplemented]` DSL 메시지 |
| 위젯 | 16종 고정 | 16종 + `Element::Custom`(제3자) |

---

## 1. 설치

```toml
[dependencies]
elm-magic = "0.8"
elm-magic-egui = "0.8"   # egui 어댑터 (선택)
elm-magic-gpui = "0.8"   # gpui-kit 어댑터 (선택)
```

`edition = "2024"` 를 쓰면 async 클로저·`use<>` 같은 최신 문법을 그대로 누린다.

---

## 2. 빠른 시작 — Todo 앱 (문법 종합)

```rust
use elm_magic::prelude::*;

#[derive(Clone, PartialEq)]
struct Todo { id: u64, text: String, done: bool }

#[derive(Clone, PartialEq)]
enum Filter { All, Active, Done }

elm_magic::store! {
    struct App { todos: Vec<Todo>, filter: Filter, draft: String, next_id: u64 }
}

elm_magic::css! {
    :root  { --brand: primary; --pad: 16; }

    .app        { height: fill; gap: 8; padding: var(--pad); }
    .row        { gap: 8; align: center; }
    .row:not(.row--done) { color: text; }
    .row--done  { opacity: 0.55; }
    .grow       { width: fill; }
    .muted      { color: text_dim; }

    @sm { .app { --pad: 8; } }   // 좁은 화면: 패딩이 변수 하나로 바뀐다
}

// 재사용 이벤트 본문 — 여러 핸들러에서 같은 로직을 쓴다 (0.8 `action!`)
elm_magic::action!(add) {
    if !app.draft.trim().is_empty() {
        app.todos.push(Todo { id: app.next_id, text: app.draft.clone(), done: false });
        app.next_id += 1;
        app.draft.clear();
    }
}

elm_magic::view! {
    fn TodoApp() {
        // 화면 본문에서 파생값을 만든다 (`let`은 0.7부터 지원)
        let visible = app.todos.iter().filter(|t| match app.filter {
            Filter::All    => true,
            Filter::Active => !t.done,
            Filter::Done   => t.done,
        }).cloned().collect::<Vec<_>>();

        <Col class="app">
            <Row class="row">
                <Input bind={app.draft} placeholder="할 일" on_enter={add()} />
                <Button kind="primary" on_click={add()}>"추가"</Button>
                <Button kind="ghost" on_click={app.todos.clear()}>"비우기"</Button>
            </Row>

            <For each={visible} as={todo} key={todo.id}>
                <Row class={cls!("row", "row--done": todo.done)}>
                    <Check bind={todo.done} />
                    <Text class="grow">{todo.text}</Text>
                    <Button kind="ghost" on_click={app.todos.remove(todo)}>"×"</Button>
                </Row>
            </For>

            <If when={visible.is_empty()}>
                <Text class="muted">"표시할 항목이 없습니다"</Text>
            </If>

            <Row class="row">
                <Switch on={app.filter}>
                    <Case when={Filter::All}>   <Tab active on_click={app.filter = Filter::All}>"전체"</Tab></Case>
                    <Case when={Filter::Active}><Tab on_click={app.filter = Filter::Active}>"진행"</Tab></Case>
                    <Default>                   <Tab on_click={app.filter = Filter::Done}>"완료"</Tab></Default>
                </Switch>
            </Row>
        </Col>
    }
}
```
헤드리스 테스트:

```rust
#[test]
fn todo_flow() {
    let mut app = elm_magic::mount!(TodoApp);

    app.type_into(&sel!(tag Input), "우유 사기");
    app.press_key("Enter");
    app.expect_text("우유 사기");

    app.click(&sel!(role Button, "전체"));   // 필터 전환
    app.expect_snapshot!("todo_all");        // 스냅샷 회귀
}
```

egui에 그리기:

```rust
fn main() -> eframe::Result<()> {
    let mut ctx = elm_magic::Ctx::default();
    eframe::run_simple_native("todo", Default::default(), |ctx_egui, _| {
        egui::CentralPanel::default().show(ctx_egui, |ui| {
            let tree = elm_magic::frame::<TodoApp>(&mut ctx, &TodoAppProps::default());
            elm_magic_egui::render(ui, &tree, &mut ctx.arena);
        });
    })
}
---

## 3. 화면은 함수 본문 — 짧아진 문법

### 3.1 제어 흐름 태그

```rust
// 조건
<If when={n > 0}>
    <Text>"양수"</Text>
<Else>
    <Text>"0 이하"</Text>
</Else>
</If>

// 반복 — keyed 슬롯 자동 발급
<For each={todos} as={t} key={t.id}>
    <Row><Check bind={t.done} />{t.text}</Row>
</For>

// 다분기 — match의 UI 버전
<Switch on={status}>
    <Case when={Status::Idle}>"대기"</Case>
    <Case when={Status::Loading}><Spinner /></Case>
    <Case when={Status::Failed(e)}><Banner kind="error">{e}</Banner></Case>
    <Default>"—"</Default>
</Switch>

// 프래그먼트 — 레이아웃 없이 묶기
<>
    <Text>"a"</Text>
    <Text>"b"</Text>
</>
```

`<For>`는 `key`가 없으면 위치 기반, 있으면 keyed 슬롯으로 동작한다(0.7의 key 규약 그대로).

### 3.2 `IntoView` — 중괄호와 `.map()` 제거

```rust
<Col>
    {title}         // &str / String
    {count}         // i32 / f64 (자동 표시)
    {maybe_user}    // Option<User> → None이면 아무것도 안 그림
    {todos}         // Iterator<Item = Todo> / Vec<Todo>
    {if debug { <Text>"debug"</Text> }}   // 기존 조건식도 그대로
</Col>
```
`IntoView`는 `&str`·`String`·숫자·`Element`·`Vec<Element>`·`Option<T>`·`Iterator<Item = T>`에 구현된다. 자식 자리는 `impl IntoView`이므로 **`.map(|x| ...)` 없이 컬렉션을 그대로** 넘긴다.

### 3.3 양방향 바인딩 `bind`

```rust
<Input    bind={name} placeholder="이름" />
<TextArea bind={bio} rows={4} />
<Check    bind={done}>"완료"</Check>
```
`value`/`on_change` 한 쌍을 대신한다. `bind`는 `String`·`bool`·`Option<T>` 슬롯에 동작한다.

### 3.4 보간 연산자 `?.` / `??` / `|>`

```rust
"안녕 {user?.name ?? \"게스트\"}님"
"{price:2}"                       // 소수 2자리
"{date:short}"                    // 로케일 포맷

items |> filter(|t| !t.done) |> each(<Row>{t.text}</Row>)
"  {text |> trim |> uppercase}  "
```

### 3.5 동적 클래스 `cls!`

```rust
<Col class={cls!("card", "card--hot": temp > 30, "card--open": open, size)}>
<Row class={["row", active.then_some("row--active")]}>
```
`css!`의 BEM 수정자 등록과 짝을 이룬다.

### 3.6 인라인 스타일 `style!`

일회성/프로토타입/테스트에서 클래스를 만들지 않고:

```rust
<Col  style!{ pad(8).gap(4).fill().bg(surface).radius(8).border(1, border) }>
<Text style!{ size(20).bold().color(error).underline() }>
```
`style!`은 `css!`와 **같은 `ResolvedStyle`** 로 해석된다(별도 경로가 아님).

### 3.7 슬롯 — 네임드 children

```rust
elm_magic::view! {
    fn Card(title: impl Into<String>, footer = None) {
        <Col class="card">
            <Slot name="header">{title.into()}</Slot>
            <Slot name="body">{children}</Slot>
            <If when={footer.is_some()}>
                <Slot name="footer">{footer}</Slot>
            </If>
        </Col>
    }
}

<Card title="프로필">
    <Slot name="header"><Text>"김철수"</Text></Slot>
    <Slot name="body"><Text>"상세 내용"</Text></Slot>
    <Slot name="footer"><Button>"저장"</Button></Slot>
</Card>
```

### 3.8 컴포넌트 선언이 짧아진다

```rust
// 0.7 — Props 구조체 + Option 필드를 손으로
elm_magic::view! {
    fn Badge(text: String, tone: Tone, children) { ... }
}

// 0.8 — impl Into + 기본값, 매크로가 <Badge>Props를 생성
elm_magic::view! {
    fn Badge(text: impl Into<String>, tone: Tone = Tone::Info, children) {
        <Text class={cls!("badge", tone.class())}>{text.into()}</Text>
    }
}
```
---

## 4. 상태와 효과

### 4.1 async 클로저 (edition 2024)

```rust
// 0.7 — 효과는 문장(`<-`)으로만
elm_magic::view! {
    fn Save() {
        <Button on_click={saved <- save()}>"저장"</Button>
    }
}

// 0.8 — 핸들러 안에서 직접 async
<Button on_click={async { saved = save().await }}>"저장"</Button>

// 낙관적 업데이트 + 실패 롤백도 한 곳에
<Button on_click={async {
    let before = n;
    n += 1;
    if sync(n).await.is_err() { n = before; }
}}>"+"</Button>
```

기존 `<-` 문장형 효과는 그대로 유지된다(호환).

### 4.2 `<Await>` — 로딩/에러/성공 3상태

```rust
<Await future={load_users()}>
    loading={<Spinner />}
    error={|e| <Banner kind="error">{e}</Banner>}
>
    {|users|
        <For each={users} as={u} key={u.id}><Text>{u.name}</Text></For>
    }
</Await>
```
`<Await>`는 `on_mount` 효과 + 상태 슬롯 + 3분기를 하나로 묶는다.

### 4.3 선언적 단축키·라우팅

```rust
elm_magic::keys! {
    "Ctrl+S" => save(),
    "Ctrl+Z" => app.undo(),
    "Escape" => app.close(),
}

elm_magic::routes! {
    "/"          => Home,
    "/user/:id"  => |id: u32| User(id),
    "*"          => NotFound,
}

// 라우팅 위젯
<Link to="/user/42">"프로필"</Link>
<Link to={Route::User(42)}>"타입 있는 이동"</Link>
```

### 4.4 상태 슬롯 문법 (기존 유지)

```rust
n += 1            // 증가
n -= 1            // 감소
x = y             // 대입
done = !done      // 토글
items.push(t)     // Vec
items.remove(t)
name = _          // 콜백 인자 바인딩
a, b <- fetch()   // 다중 타깃 효과
```

### 4.5 `action!` — 재사용 가능한 이벤트 본문

여러 핸들러에서 같은 로직을 쓸 때, 이벤트 본문을 이름 붙여 분리한다.

```rust
elm_magic::action!(add) {
    if !draft.trim().is_empty() {
        todos.push(Todo { id: next_id, text: draft.clone(), done: false });
        next_id += 1;
        draft.clear();
    }
}

// 사용
<Input bind={draft} on_enter={add()} />
<Button on_click={add()}>"추가"</Button>
```

`action!`은 컴포넌트 상태 슬롯(또는 `store!` 필드)을 캡처하므로, `let`/클로저로 상태를 우회하지 않고도 로직을 한 곳에 둘 수 있다.

---

## 5. 스타일 — `css!` 확장

### 5.1 새 문법

```rust
elm_magic::css! {
    // 2축 gap / padding / margin: auto
    .toolbar  { gap: 8 16; padding: 8 16; margin: auto; }
    .hero     { width: 100%; aspect-ratio: 16/9; }

    // 방향별 radius
    .card     { radius: 12 12 0 0; }

    // CSS 변수 + 참조
    :root     { --brand: primary; --pad: 16; }
    .btn      { bg: var(--brand); padding: var(--pad); }

    // 컴포지션
    .chip     { padding: 2 8; radius: 999; bg: surface_alt; }
    .badge    { @extends .chip; color: text_dim; }

    // 의사 클래스 확장
    .row:first-child   { border-width: 0; }
    .row:nth-child(2n) { bg: surface_alt; }
    .btn:not(.btn--ghost) { color: on_primary; }
    .btn:hover         { bg: surface_alt; }

    // 브레이크포인트 (= @media (max-width: 480))
    @sm { .toolbar { gap: 4; } }
    @md { .toolbar { gap: 8 16; } }
    @lg { .hero { width: 60%; } }
}
```

### 5.2 컴포넌트 스코프드 CSS

CSS와 뷰를 같은 함수 안에 두고, 클래스 이름 충돌 없이:

```rust
elm_magic::view! {
    fn Card(title: impl Into<String>) {
        css! {
            .card       { padding: 12; bg: surface; radius: 12; shadow: 0 2 8 shadow; }
            .card:hover { bg: surface_alt; }
        }
        <Col class="card">{title.into()}</Col>
    }
}
```
스코프가 붙은 클래스는 내부적으로 해시되어(`card__a13f`) 다른 `Card`의 스타일과 섞이지 않는다.

### 5.3 팔레트/테마

```rust
// egui: 밝기 설정을 따름 / gpui: ActiveTheme을 따름 — 그대로 유지
elm_magic::render(ui, &tree, &mut ctx.arena);

// 커스텀 테마도 동일
let theme = Palette::dark().with(Token::Primary, Color::rgb(1, 2, 3));
elm_magic_egui::render_with_palette(ui, &tree, &mut ctx.arena, &theme);
```
---

## 6. 나만의 태그와 위젯

0.8은 **"매크로가 매크로를 정의하는" 계층**을 제공한다. 세 층으로 나뉜다.

| 층 | 도구 | 어댑터 수정 | 대상 |
|---|---|---|---|
| 복합 태그 | `define_tag!` / `variants!` | **불필요** | 앱 개발자 (신규 위젯 80%) |
| 원시 위젯 | `widgets!` | `Bridge` 1회 | 프레임워크/디자인시스템 |
| 제3자 원시 | `Element::Custom` | 필요 | 외부 크레이트 생태계 |

### 6.1 `define_tag!` — 기존 primitive로 desugar되는 복합 태그

```rust
elm_magic::define_tag! {
    /// 프라이머리 버튼: 클래스·children까지 감싼 태그.
    tag PrimaryButton(on_click, class = "", children) {
        <Button class={cls!("btn btn--primary", class)} on_click={on_click}>
            {children}
        </Button>
    }

    /// 아이콘 + 라벨을 한 덩어리로.
    tag IconButton(icon: impl Into<String>, label: impl Into<String>, on_click) {
        <Row class="row">
            <Text class="icon">{icon.into()}</Text>
            <Button on_click={on_click}>{label.into()}</Button>
        </Row>
    }
}

// 사용 — Props 구조체도, Component impl도 없다
<PrimaryButton on_click={save()}>"저장"</PrimaryButton>
<IconButton icon="★" label="즐겨찾기" on_click={star()} />
```
`define_tag!`은 **실제 primitive 엘리먼트로 인라인 확장**되므로 egui/gpui 어댑터를 전혀 건드리지 않는다.

### 6.2 `variants!` — 태그 "가족"을 자동 생성

```rust
elm_magic::variants! {
    Button as Btn {
        kind { primary => "btn--primary", ghost => "btn--ghost", danger => "btn--danger" },
        size { sm => "btn--sm", md => "btn--md", lg => "btn--lg" },
    }
}

<Btn kind="primary" size="sm" on_click={save()}>"저장"</Btn>
<Btn kind="danger"  size="lg" on_click={del()}>"삭제"</Btn>
```
`:kind`/`:size`는 **컴파일 타임에 유효값이 검사**된다 — 잘못된 값은 컴파일 에러(오타 방지 + IDE 자동완성).

### 6.3 `widgets!` — 원시 위젯을 한 곳에서 선언 (프레임워크 개발자)

0.7에서는 새 위젯마다 **5곳**(`Element` variant · `Widget` impl · `enum_dispatch` · `is_builtin_tag` · 어댑터 `match`)을 고쳤다. 0.8은 **한 선언**이다.

```rust
elm_magic::widgets! {
    Slider {
        value: f64,
        min: f64 = 0.0,
        max: f64 = 1.0,
        on_change: Event<f64>,
        role: Slider,
    }

    Rating {
        value: u8,
        max: u8 = 5,
        on_change: Event<u8>,
        role: Rating,
    }
}
```
이 선언 하나가 생성하는 것:
- `Element::Slider(SliderEl)` / `Element::Rating(RatingEl)` variant
- 정적 디스패치(`enum_dispatch` 대체, `match` 직접 생성)
- `view!`의 태그 인식 목록 등록 → `<Slider ... />` 즉시 사용 가능
- Props 기본값(`<SliderProps>::default()`)
- 접근성 `Role`·라벨 연결

### 6.4 `Bridge` — 어댑터의 변형 `match` 제거 (어댑터 개발자)

0.8의 핵심 구조 변경. 어댑터는 **위젯마다가 아니라 플랫폼마다 한 번** 구현한다.

```rust
// 코어가 정의 (플랫폼 중립)
pub trait Bridge {
    type Node;                                              // 플랫폼 노드
    fn text(&mut self, el: &TextEl, st: &ResolvedStyle) -> Self::Node;
    fn button(&mut self, el: &ButtonEl, st: &ResolvedStyle, label: Self::Node) -> Self::Node;
    fn container(&mut self, el: &ColEl, st: &ResolvedStyle, children: Vec<Self::Node>) -> Self::Node;
    fn custom(&mut self, el: &CustomEl, st: &ResolvedStyle) -> Self::Node;
    // … primitive마다 하나
}
```

egui 어댑터는 위 `Bridge`를 한 번 구현하고 끝:
```rust
struct EguiBridge<'a> { ui: &'a mut egui::Ui, pass: &'a mut Pass }

impl Bridge for EguiBridge<'_> {
    type Node = egui::Response;
    fn text(&mut self, el: &TextEl, st: &ResolvedStyle) -> egui::Response { /* … */ }
    fn button(&mut self, el: &ButtonEl, st: &ResolvedStyle, label: egui::Response) -> egui::Response { /* … */ }
    // …
}
```
이제 새 위젯(`Slider`)을 추가해도 **어댑터 두 벌을 고칠 필요가 없다** — 위젯이 `Bridge::slider` 한 메서드를 코어에 추가하면 egui·gpui 모두 같은 이름으로 구현만 한다.

### 6.5 `Element::Custom` — 제3자 원시 위젯 (생태계)

```rust
#[derive(Serialize, Deserialize)]   // serde 기능에서는 Custom이 제외된다
struct ChartEl { points: Vec<(f64, f64)> }

impl CustomWidget for ChartEl {
    fn tag() -> &'static str { "Chart" }
    fn render(&self, bridge: &mut dyn BridgeDyn) -> Handled { /* 플랫폼별 paint */ }
}
```
> ⚠️ `Element: Serialize` 계약과 충돌하므로 `Custom`은 `--features serde`에서 직렬화 대상이 아니다(문서화된 예외).

### 6.6 컴파운드: 직접 만든 태그를 앱 전체에서

```rust
// 한 번 정의 → 어디서나
elm_magic::define_tag! {
    tag StatCard(label: impl Into<String>, value: impl Into<String>) {
        <Col class="stat">
            <Text class="stat__label">{label.into()}</Text>
            <Strong class="stat__value">{value.into()}</Strong>
        </Col>
    }
}

<Row class="row">
    <StatCard label="매출"   value="{revenue:currency}" />
    <StatCard label="방문자" value="{visits}" />
</Row>
```
---

## 7. 어댑터 사용 (egui / gpui)

```rust
// egui
use elm_magic::prelude::*;

fn ui(ui: &mut egui::Ui, ctx: &mut Ctx) {
    let tree = elm_magic::frame::<TodoApp>(ctx, &TodoAppProps::default());
    elm_magic_egui::render(ui, &tree, &mut ctx.arena);
}
```

```rust
// gpui-kit — css! 팔레트는 ActiveTheme에서 온다
use elm_magic_gpui::ElmView;

div().child(cx.new(|cx| ElmView::<TodoApp>::new(cx)))
```

`<Raw>` 탈출구도 그대로:
```rust
<Raw>|ui: &mut egui::Ui| { ui.hyperlink_to("docs", "https://example.com"); }</Raw>
```

---

## 8. 테스트와 진단

### 8.1 헤드리스 테스트 API

```rust
let mut app = elm_magic::mount!(TodoApp);

app.click("추가");
app.click_sel(&sel!(role Button, "추가"));
app.click_role(Role::Button, "추가");
app.type_into(&sel!(tag Input), "우유");
app.press_key("Ctrl+S");
app.expect_text("우유");
assert!(app.exists(&sel!(.row--done)));
println!("{}", app.a11y_tree());

app.advance(300.millis());   // 디바운스/타이머
app.expect_snapshot!("todo_default");   // insta 스냅샷
```

### 8.2 에러는 DSL 언어로

```rust
elm_magic::view! {
    fn Bad() {
        <Col>{widget}</Col>   // widget: 엘리먼트가 아님
    }
}
```
```
error: `MyWidget`는 화면에 그릴 수 없습니다
  --> src/ui.rs:12:14
   |
12 |         <Col>{widget}</Col>
   |              ^^^^^^
   = help: `IntoView`를 구현하거나 `.into_view()`를 쓰세요
   = note: 그릴 수 있는 타입: &str, String, 숫자, Element, Vec<Element>, Option<T>, Iterator<Item = T>
```

잘못된 태그/속성:
```
error: 알 수 없는 태그 `<Сol>` (키릴 문자 'С')
  = help: 비슷한 태그: `<Col>`. 내장 태그: Col, Row, Text, Strong, Button, Input, …
```

### 8.3 `#[diagnostic::on_unimplemented]`

트레이트 경계 에러를 DSL 어휘로 번역:
```rust
#[diagnostic::on_unimplemented(
    message = "`{Self}`는 화면에 그릴 수 없습니다",
    label = "`IntoView`를 구현하세요",
)]
pub trait IntoView { fn into_view(self) -> Element; }
```

---

## 9. 마이그레이션 (0.7 → 0.8)

0.8은 **추가 중심**이라 기존 코드는 대부분 그대로 돈다.

| 0.7 코드 | 0.8 권장 |
|---|---|
| `{if c { <A/> } else { <B/> }}` | `<If when={c}><A/><Else><B/></Else></If>` |
| `{items.map(\|i\| <Row>{i}</Row>)}` | `<For each={items} as={i}><Row>{i}</Row></For>` |
| `value={x.clone()} on_change={x = _}` | `bind={x}` |
| `class="a"` | `cls!("a", "a--on": cond)` |
| `value={n}` (숫자) | `{n}` |
| `Element::Button(..)` 매칭 | `el.widget_kind()` / `Widget` 메서드 (변화 없음) |
| 커스텀 위젯 5곳 수정 | `widgets!` 한 곳 |
| 어댑터 `match` | `Bridge` 구현 |

**제거/변경 없음**: `mount!`, `sel!`, `store!`, `<-` 효과, `css!` 셀렉터/상태/캐스케이드, `Widget` 트레이트, `--features serde`.

**필요 조건**: `edition = "2024"` (async 클로저/`use<>` 사용 시). MSRV 1.85.

---

## 10. MSRV · 의존성 · 로드맵

| 항목 | 값 |
|---|---|
| edition | 2024 |
| MSRV | **1.85** |
| let-chains 사용 시 | 1.88 (선택) |
| 런타임 의존성 | **0** (`cargo tree -p elm-magic -e normal` = 매크로만) |
| 컴파일타임 의존성 | `elm-magic-macros`, (`widgets!` 도입 시 `enum_dispatch` 제거 가능) |
| 선택 기능 | `serde` (`Element: Serialize`, `Custom` 제외) |

**단계**
1. **문법 팩** — edition 2024, `IntoView`, 제어 흐름 태그, `bind`, `cls!`, `?.`/`|>`, `#[diagnostic]`
2. **스타일 팩** — `css!` 확장(`@media`/변수/`@apply`/`:not`), `style!`, 스코프드 CSS
3. **위젯 팩** — `widgets!` 단일 선언, `Bridge`(중립 `Response`/`Rect`), `define_tag!`/`variants!`
4. **생태계 팩** — `Element::Custom`, `<Await>`, `keys!`/`routes!`, `<Link>`/`<Tooltip>`/`<Table>`

각 단계는 `cargo test --workspace` + `trybuild` 실패 계약 + 어댑터 헤드리스 테스트 + `cargo tree -e normal` 0 검사를 그대로 통과해야 한다.

---

> 이 문서는 **프리뷰**다. 확정 문법·시그니처는 0.8 개발 중 `CHANGELOG.md`, `README.md`,
> `tests/diagnostics.rs`, `tests/compile_fail/*.stderr`가 계약으로 고정한다.
```
